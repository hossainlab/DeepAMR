import { LoginForm } from "@/components/features/auth/login-form";

export const metadata = {
  title: "Sign In - DeepAMR",
  description: "Sign in to your DeepAMR account",
};

export default function LoginPage() {
  return <LoginForm />;
}
